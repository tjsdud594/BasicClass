학습내용

1. encoding
      - utf-8
      - python 3 버전은 기본적으로 utf-8

2.개발환경구축
    1.anaconda
        통합개발 배포환경
        개발 tool
        다양한 library들 이미 기본적으로 제공
    2.vsc
        anaconda설치전 설치 권장
        python plug in 설치
3.tip
    1. 윈도우 화면분할
         윈도우키+방향키
    2. cmd창 오픈
        ctrl + r -> 실행창 실행 -> cmd 입력
    3. vsc상에서 
        explorer 창 활성/비활성 단축기
           ctrl + b
    4. 윈도우 탐색기 창 띄우기
           ctrl + e